export interface BlockAssignment {
  id: string;
  academicYear: string;
  blockId: string;
  blockNumber: number;
  residentId: string;
  residentName: string;
  rotationId: string;
  rotationName: string;
  notes: string;
  createdAt: string;
  updatedAt: string;
}